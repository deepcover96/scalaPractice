package Util


object Math {
  /**
    * Long division. Divide dividend by divisor.
    * Repeated remainders should be terminated with parenthesis
    * @param num dividend
    * @param d divisor
    */
  def longDivision(num: Int, d: Int): String = {
    var result: Int = 0
    var remainder: String = ""

    result.toString + remainder
  }
}
